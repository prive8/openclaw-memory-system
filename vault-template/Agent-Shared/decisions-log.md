# Decisions Log

<!--
Append-only log of durable decisions. Format:

- **<short title>** _(<context>_). <what was decided>. <why>. <what changed as a result>. <YYYY-MM-DD>.

A decision is durable if it would still matter 6 months from now —
architecture choices, scope decisions, "we'll never do X again" rules.
Skip trivial session-level calls; those live in daily/.
-->

- **First decision** _(bootstrap)_. Placeholder entry; replace with a real one. Format example: chose pattern X over Y because Z; rule A retired; approach B locked. 2026-08-21.
